I'll guide you step by step to configure a **Zone-Based Policy Firewall (ZPF) in Cisco Packet Tracer** and ensure it works correctly.

---

## **Step 1: Verify Network Connectivity**
Before configuring the firewall, confirm that all devices can communicate.

1. **Ping PC-C from PC-A**
   - Open PC-A’s command prompt.
   - Run:  
     ```
     ping 192.168.3.3
     ```
   - If it succeeds, proceed. If it fails, check routing.

2. **SSH into R2 from PC-C**
   - Open PC-C’s command prompt.
   - Run:  
     ```
     ssh -l Admin 10.2.2.2
     ```
   - Enter the password: `Adminpa55`
   - If SSH works, type `exit` to log out.

3. **Access PC-A’s Web Server from PC-C**
   - Open the Web Browser on PC-C.
   - Enter: `http://192.168.1.3`
   - If the Packet Tracer Welcome Page appears, proceed.

---

## **Step 2: Enable Security Features on R3**
1. Open **R3’s CLI**.
2. Enter **privileged EXEC mode**:
   ```
   enable
   ```
   - Password: `ciscoenpa55`
3. Go to **global config mode**:
   ```
   configure terminal
   ```
4. Check if the **Security License** is enabled:
   ```
   show version
   ```
5. If not enabled, activate it:
   ```
   license boot module c1900 technology-package securityk9
   ```
6. Accept the license agreement.
7. Save the configuration and reload:
   ```
   write memory
   reload
   ```
8. After reboot, verify:
   ```
   show version
   ```

---

## **Step 3: Create Firewall Zones**
1. Create the **Internal Zone**:
   ```
   configure terminal
   zone security IN-ZONE
   exit
   ```
2. Create the **External Zone**:
   ```
   zone security OUT-ZONE
   exit
   ```

---

## **Step 4: Define Traffic to Be Inspected**
1. Create an **Access Control List (ACL)** for internal traffic:
   ```
   access-list 101 permit ip 192.168.3.0 0.0.0.255 any
   ```
2. Create a **Class Map** referencing the ACL:
   ```
   class-map type inspect match-all IN-NET-CLASS-MAP
   match access-group 101
   exit
   ```

---

## **Step 5: Define Firewall Policies**
1. Create a **Policy Map**:
   ```
   policy-map type inspect IN-2-OUT-PMAP
   ```
2. Attach the **Class Map** to the policy:
   ```
   class type inspect IN-NET-CLASS-MAP
   ```
3. Enable traffic **inspection**:
   ```
   inspect
   exit
   exit
   ```

---

## **Step 6: Apply the Firewall Policy**
1. Create a **Zone Pair**:
   ```
   zone-pair security IN-2-OUT-ZPAIR source IN-ZONE destination OUT-ZONE
   ```
2. Attach the **Policy Map**:
   ```
   service-policy type inspect IN-2-OUT-PMAP
   exit
   ```

---

## **Step 7: Assign Interfaces to Zones**
1. Assign **G0/1 (LAN) to IN-ZONE**:
   ```
   interface g0/1
   zone-member security IN-ZONE
   exit
   ```
2. Assign **S0/0/1 (WAN) to OUT-ZONE**:
   ```
   interface s0/0/1
   zone-member security OUT-ZONE
   exit
   ```

---

## **Step 8: Save and Verify Configuration**
1. Save configuration:
   ```
   write memory
   ```
2. Verify:
   ```
   show zone security
   show policy-map type inspect zone-pair sessions
   ```

---

## **Step 9: Test the Firewall**
1. **Ping PC-A from PC-C** – should succeed.
2. **SSH into R2 from PC-C** – should succeed.
3. **Open PC-A’s Web Page on PC-C** – should succeed.
4. **Ping PC-C from PC-A** – should fail.
5. **Ping PC-C from R2** – should fail.

---

### **Final Step: Check Results**
- Click **Check Results** in Packet Tracer.
- Your completion should be **100%**.

Let me know if you get stuck! 🚀